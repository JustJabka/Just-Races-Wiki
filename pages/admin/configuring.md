# Configuring

All configuration files are located in the `plugins/JustRaces/` folder: **races**, **abilities**, **traits**, **modifiers**

After making changes to any configuration file, you **DON'T** need to restart the entire server. Simply run the reload command in-game or from the console:

```bash
/justraces reload

```